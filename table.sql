/*Automatically generated code from PostgreSQL */

CREATE TABLE public.scores
(
    score integer,
    username character varying(15) COLLATE pg_catalog."default"
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public.scores
    OWNER to postgres;
